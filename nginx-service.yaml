apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: Loadbalancer
  selector:
    app: nginx
  ports:
    - protocol: "TCP"
      port: 80
      nodePort: 30080
      name: http
    - port: 443
      name: https
